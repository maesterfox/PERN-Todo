{
    "build": {
      "commands": [
        "npm install",
        "npm install --prefix client",
        "npm run build --prefix client"
      ]
    },
    "start": {
      "command": "node server/index.js"
    },
    "env": {
      "PORT": 5000
    }
  }
  